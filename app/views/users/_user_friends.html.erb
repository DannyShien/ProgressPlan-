<div class="card">
  <div class="card-content">
    <article class="media">
      <figure class="media-left"><%= image_tag user.image_url_or_default %></figure>
      <div class="media-content">
        <div>
          <p class="subtitle">
            <strong><%= user.name %></strong>
          </p>
          <div class="media-right">
            <% if current_user.is_friend?(user) %>
              <%= link_to icon("trash-o", " "), remove_friend_path(user_id: user), class: "button  is-danger", method: :delete %>              
            <% else %>
              <%= form_for current_user.friendships.build(friend: user) do |f| %>
                <%= f.hidden_field :friend_id %>
                <%= f.submit "Add Friends", class: "button is-small is-info" %>
            <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </article>
  </div>
</div>